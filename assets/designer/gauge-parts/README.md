Drop reusable gauge component assets here: needles, hubs, tick styles, bezels, warning bands, labels and other SVG/PNG/JPG/WEBP parts.

The long-term gauge builder will treat these as components attached to a gauge container. The container owns the geometry; components conform to that geometry so the same tick/needle system can work on round, oval, horizontal, vertical or custom-path gauges.
